import * as schedule from '../src/schedule';

describe('schedule', () => {
  describe('.scheduleJob', () => {
    context('when name is specified', () => {
      it('should schedule an anonymous job', () => {
        // const job = schedule.scheduleJob(() => {}, '*/1 * * * * *');
      });
    });
  });
});
